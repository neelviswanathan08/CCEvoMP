% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize_results.R
\name{subset_time_series}
\alias{subset_time_series}
\title{Subset time series data to a specific time range}
\usage{
subset_time_series(file_path, start_time, end_time)
}
\arguments{
\item{file_path}{Path to the CSV file containing trait data}

\item{start_time}{Start time for the subset}

\item{end_time}{End time for the subset}
}
\value{
A data frame containing the subsetted data
}
\description{
Subset time series data to a specific time range
}
\examples{
\dontrun{
# Create a sample dataset
sample_data <- data.frame(
  time = 1:100,
  mn = cumsum(rnorm(100)),
  sd = runif(100, 0.1, 0.5),
  n = 10
)

# Write to a temporary file
temp_file <- tempfile(fileext = ".csv")
write.csv(sample_data, temp_file, row.names = FALSE)

# Use the function
subset_data <- subset_time_series(temp_file, 20, 80)

# Clean up
unlink(temp_file)
}
}
